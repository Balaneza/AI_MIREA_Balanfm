{
  "experiment_id": "C4",
  "dataset": "STL10",
  "num_classes": 10,
  "seed": 42,
  "architecture": "ResNet18-finetune",
  "unfrozen_layers": ["layer4", "fc"],
  "optimizer": "Adam",
  "lr": 0.0001,
  "weight_decay": 0.0001,
  "batch_size": 64,
  "max_epochs": 40,
  "patience": 8,
  "epochs_trained": 12,
  "best_val_accuracy": 0.956,
  "test_accuracy": 0.9524,
  "preprocess": {
    "type": "torchvision.transforms",
    "normalize": {
      "mean": [0.485, 0.456, 0.406],
      "std": [0.229, 0.224, 0.225],
      "note": "ImageNet normalization for ResNet18 pretrained weights"
    }
  },
  "transforms_train": [
    "RandomHorizontalFlip (p=0.5)",
    "RandomVerticalFlip (p=0.5)",
    "RandomCrop (size=96, padding=12)",
    "ColorJitter (brightness=0.2, contrast=0.2, saturation=0.2)",
    "RandomGrayscale (p=0.1)",
    "Normalize (ImageNet stats)"
  ],
  "transforms_val": [
    "Normalize (ImageNet stats)"
  ],
  "augmentation": {
    "enabled": true,
    "apply_to": "train",
    "strategy": "random augmentations to increase data variance and reduce overfitting"
  },
  "notes": "layer4+fc unfrozen; aug train; ResNet18 with ImageNet pretrained weights"
}